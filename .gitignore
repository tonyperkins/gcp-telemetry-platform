# ---- Go ----
# Compiled binaries (built by Docker/Cloud Build, not committed)
/server
/telemetry-server
/flightpusher
*.exe
*.test
*.out

# ---- Node / frontend ----
node_modules/
dashboard/node_modules/
# Built SPA is generated by the Docker build; not committed
dashboard/dist/
npm-debug.log*
yarn-error.log*

# ---- Terraform ----
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
# Keep the provider lock file committed (remove the next line to commit it):
# .terraform.lock.hcl

# ---- Secrets / credentials ----
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example
*-key.json
*_key.json
service-account*.json
credentials*.json
gcp-*-key.json
*.pem

# ---- Logs / misc ----
*.log
out.log

# ---- OS / editor ----
.DS_Store
Thumbs.db
.idea/
*.swp
blog/